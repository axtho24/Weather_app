let assert = require('assert')
let color = require('../testing').color

describe('sum', function() {
    it('should return color red', function() {
        temp = 90
        let a = color(temperature)
        assert.equal(a, 'red')
    })
})
